{{ config(
  description = 'OM_DEMAND_SHAPING_RULES_DIM view for Sales Analytics Team', 
  materialized = 'view', 
  schema = 'SALES', 
  tags = 'MART_SALES',
  alias = 'OM_DEMAND_SHAPING_RULES_DIM'
) }} 

SELECT 
    DEMAND_SHAPING_RULE_KEY,
    RULE_NAME,
    LEVEL_NAME,
    LEVEL_VALUE1,
    RULE_VALUE,
    RULE_ACTIVE_START_DATE,
    RULE_ACTIVE_END_DATE,
    RULE_CREATION_DATETIME,
    RULE_CREATED_BY,
    RULE_LAST_UPDATED_DATETIME,
    RULE_LAST_UPDATED_BY,
    LEVEL_VALUE2,
    LEVEL_VALUE3,
    IS_RULE_ACTIVE,
    BIW_INS_DTTM,
    BIW_UPD_DTTM,
    BIW_BATCH_ID,
    BIW_MD5_KEY
FROM 
  {{ref('MART_SALES_OM_DEMAND_SHAPING_RULES') }} 
