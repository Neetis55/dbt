{{ 
config
(
  description = 'NPD LANDING PAGE REPORT VIEW ', 
  materialized = 'view', 
  schema = 'NPD', 
  tags = ['MART_NPD'],
  alias = 'PROJECTS_RISK_RPT'
) 
}}

SELECT 
    PROJECT_RISK_KEY,
    SNAPSHOT_WEEK_KEY,
    PROJECT_NUMBER,
    PROJECT_NAME,
    BUSINESS_UNIT,
    DIVISION,
    DIVISION_PRIORITY,
    PROGRAM_MANAGER,
    TO_ACT,
    RISK_TYPE,
    RISK,
    RISK_DESCRIPTION,
    TOP_PROJECT,
    ADDITIONAL_PROJECT_LINKS,
    CREATED,
    CREATED_BY,
    MODIFIED,
    MODIFIED_BY,
    BIW_INS_DTTM ,
    BIW_UPD_DTTM ,
    BIW_BATCH_ID,
    BIW_MD5_KEY
FROM 
    {{ref ('MART_NPD_PROJECTS_RISK_RPT')}}
WHERE SNAPSHOT_WEEK_KEY > (SELECT (SUBSTRING(MAX(SNAPSHOT_WEEK_KEY)-200,1,4)||'00')::NUMBER FROM {{ref ('MART_NPD_PROJECTS_RISK_RPT')}})