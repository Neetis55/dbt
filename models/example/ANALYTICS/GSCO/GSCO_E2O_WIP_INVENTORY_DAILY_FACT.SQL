{{ config(
  description = 'E2open view for E2O_WIP_INVENTORY_DAILY_FACT table', 
  materialized = 'view', 
  schema = 'GSCO', 
  tags = ['E2OPEN'],
  alias = 'E2O_WIP_INVENTORY_DAILY_FACT'
) }} 

SELECT 
    SYSTEM_LOT_NUMBER,
    SNAPSHOT_DATE_KEY,
    LOT_KEY,
    LOCATION_KEY,
    PART_KEY,
    STAGE_SET_KEY,
    TOTAL_WAFER_QUANTITY,
    GOOD_WAFER_QUANTITY,
    REJECT_WAFER_QUANTITY,
    TOTAL_DIE_QUANTITY,
    GOOD_DIE_QUANTITY,
    REJECT_DIE_QUANTITY,
    LOT_HOLD_TRANSACTION_TIME_AGING_DAY,
    AGING_DAY,
    BIW_INS_DTTM ,
    BIW_UPD_DTTM ,
    BIW_BATCH_ID 
FROM 
{{ref('MART_E2OPEN_E2O_WIP_INVENTORY_DAILY_FACT')}} 

